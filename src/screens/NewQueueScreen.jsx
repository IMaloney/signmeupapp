import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const NewQueueScreen = ({ navigation }) => {
	return (
		<SafeAreaView>
			<Text>New Queue Screen</Text>
		</SafeAreaView>
	);
};

export default NewQueueScreen;